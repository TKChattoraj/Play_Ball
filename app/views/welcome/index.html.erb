<h2 class="text-center">Standings</h1>

<div class="row">
  <div class="col-md-8">
    <% @teams.each do |t| %>
      <div class="media">
        <div class="media-body">
          <h3 class="media-heading">
            <%= link_to t.name, t %>
          </h3>
        </div>
      </div>
    <% end %>
  </div>
</div>
